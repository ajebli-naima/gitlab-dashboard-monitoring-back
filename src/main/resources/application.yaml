# App properties

server:
  port: 8080
  servlet:
    context-path: /devops


spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  datasource:
    url: jdbc:mysql://localhost:3306/devopsDatabase
    username: sectorisationuser
    password: sectorisationuser
    driverClassName: com.mysql.jdbc.Driver
